{
  "name": "double-metaphone-www",
  "private": true,
  "dependencies": {
    "double-metaphone": "^1.0.1"
  },
  "devDependencies": {
    "browserify": "^16.0.0",
    "tinyify": "^2.4.3"
  },
  "scripts": {
    "build": "browserify . -p tinyify -o build.js"
  }
}
