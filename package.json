{
  "name": "double-metaphone-www",
  "private": true,
  "dependencies": {
    "double-metaphone": "^1.0.1"
  },
  "devDependencies": {
    "browserify": "^14.0.0"
  },
  "scripts": {
    "build": "browserify index.js > build.js"
  }
}
